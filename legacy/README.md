# Legacy Palantir prototype

These files are the original Foundry / Plotly Dash apply-project skeleton. They are **not** used by the AegisEdge ATC MVP.

- `webAppCodePalantir.py` — Dash monolith (Foundry dataset load + UI + algorithms)
- `generate_demo_data.py` — original demo generator (active copy lives under `backend/app/demo/`)
